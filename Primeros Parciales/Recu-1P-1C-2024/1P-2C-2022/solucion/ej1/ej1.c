#include "ej1.h"

char** agrupar_c(msg_t* msgArr, size_t msgArr_len){
    char* res[msgArr_len]; // Declaro el array
    for(uint64_t i = 0; i < msgArr_len; i++){ // Ciclo inicial para que todos sean el string vacío 
        res[i] = "";
    }
}
